# Finite exact-state tables

- `two_orbit_finite_selected.csv` — representative evaluations of the exact known-diameter formula and the remaining known-size lower/upper interval.
- `../audit/audit_formula.py` — original theorem sanity audit.
- `../audit/computational_evidence.py` — wider finite-grid generator.

The known-diameter rows are exact theorem values. The known-size rows are explicitly an interval and must not be presented as an exact formula.
